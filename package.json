{
  "name": "seguranca-digital-idosos",
  "version": "1.0.0",
  "description": "Plataforma educativa de segurança digital para idosos",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"python -m uvicorn backend.server:app --host 127.0.0.1 --port 8001 --reload\" \"cd frontend && yarn start\"",
    "start": "npm run dev",
    "start:frontend": "cd frontend && yarn start",
    "start:backend": "python -m uvicorn backend.server:app --host 127.0.0.1 --port 8001 --reload",
    "install:all": "cd frontend && yarn install && cd ../backend && pip install -r requirements.txt",
    "build": "cd frontend && yarn build"
  },
  "keywords": [
    "segurança digital",
    "idosos",
    "educação",
    "react",
    "fastapi",
    "mongodb"
  ],
  "author": "Seu Nome",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}